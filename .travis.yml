language: c
sudo: required

os:
  - linux
compiler:
  - clang
  - gcc
packages:
  - g++-4.9
  - g++-5
  - g++-6
  - g++-7

before_install:
  - sudo apt-get install gfortran

install:
  - mkdir build
  - cd build
  - cmake ../
  - make

script:
  - ls